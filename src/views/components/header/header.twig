{# ============================================================
   NAJD THEME — Header Component (Layout Router)
   Routes to the selected header style layout
   Supports: classic / centered / minimal / mega / transparent / hamburger
   Glass-aware: adapts to all 3 modes
   ============================================================ #}

{% set header_style = theme.settings.get('header_style', 'classic') %}

<header class="najd-header najd-header--{{ header_style }}" id="najd-header">

    {% if header_style == 'centered' %}
        {% include 'components.header.layouts.centered' %}
    {% elseif header_style == 'minimal' %}
        {% include 'components.header.layouts.minimal' %}
    {% elseif header_style == 'mega' %}
        {% include 'components.header.layouts.mega' %}
    {% elseif header_style == 'transparent' %}
        {% include 'components.header.layouts.transparent' %}
    {% elseif header_style == 'hamburger' %}
        {% include 'components.header.layouts.hamburger' %}
    {% else %}
        {% include 'components.header.layouts.classic' %}
    {% endif %}

</header>
